#include <iostream>

void power(int, int);
void the_switch(char);
void int_array();

int main() {
    /*
    power(2, 3);
    power(2, 8);
    power(3, 3);
    power(5, 5);
    */
    the_switch('n');
    the_switch('N');
    the_switch('y');
    the_switch('Y');
    the_switch('Z');
}